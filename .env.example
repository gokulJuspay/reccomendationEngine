# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=recommendation_engine
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Server Port
PORT=3000

# =============================================================================
# AI PROVIDER CONFIGURATION
# Choose ONE of the following options
# =============================================================================

# OPTION 1: Juspay Grid AI (Internal Juspay Use)
GRID_AI_API_KEY=your_grid_ai_key_here
# Using: https://grid.ai.juspay.net/v1/chat/completions

# OPTION 2: Google Gemini API (Direct API)
RECOMMENDATIONS_API_KEY=your_gemini_api_key_here
# Get your key from: https://aistudio.google.com/app/apikey

# =============================================================================
# NEUROLINK CONFIGURATION (Advanced - Multi-Provider Support)
# =============================================================================

# Enable Neurolink SDK (true/false)
# When true: Uses Neurolink with Google Vertex AI
# When false: Uses direct API (Gemini or Grid AI based on keys above)
USE_NEUROLINK=false

# Google Vertex AI Configuration (Required when USE_NEUROLINK=true)
# =============================================================================

# METHOD 1: Service Account File (Recommended for Production)
# Path to your service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account.json

# METHOD 2: Service Account JSON String (Good for Containers/Cloud)
# Uncomment and provide the full JSON content as a string
# GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project",...}

# METHOD 3: Individual Environment Variables (Good for CI/CD)
# Uncomment and provide individual service account details
# GOOGLE_AUTH_CLIENT_EMAIL=service-account@your-project.iam.gserviceaccount.com
# GOOGLE_AUTH_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_KEY\n-----END PRIVATE KEY-----"

# Required for ALL Google Vertex AI authentication methods
GOOGLE_VERTEX_PROJECT=your-gcp-project-id

# Optional: Override defaults
GOOGLE_VERTEX_LOCATION=us-central1
VERTEX_MODEL_ID=gemini-2.5-flash-lite

# =============================================================================
# PRIORITY ORDER (when USE_NEUROLINK=false)
# =============================================================================
# 1. RECOMMENDATIONS_API_KEY → Uses Google Gemini API directly
# 2. GRID_AI_API_KEY → Uses Juspay Grid AI as fallback
# 3. Error if neither is set
